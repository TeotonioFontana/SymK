No piece of code will cross the 600 lines boudary. If it happens, the code must be splited

Before coding any python script, it must be scaffolded by the python_code_init.py

The python script standard heading is mandatory.